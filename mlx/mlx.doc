1.) mlx_init()
    - Initialize mlx object
    - Return void pointer (mlx_ptr)
    - Doesn't do anyrthing except init
    - fail if return NULL

2.) mlx_new_window(void *mlx, int x, int y, char *title)
    - Create Window Object
    - take x as width, y as height
    - Return void pointer (mlx_window_ptr)
    - fail if return NULL

3.) mlx_loop(void *mlx)
    - Use for initiate the window rendering
    


Cflag = -framework OpenGL -framework AppKit